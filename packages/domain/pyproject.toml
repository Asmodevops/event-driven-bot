[project]
name = "domain"
version = "0.1.0"
description = "Доменные сущности q_bot (без зависимостей от БД и фреймворков)."
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/domain"]
